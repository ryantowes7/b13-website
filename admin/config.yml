backend:
  name: git-gateway
  branch: main   # sesuaikan branch repo kamu

publish_mode: editorial_workflow

media_folder: "images/uploads"
public_folder: "/images/uploads"

site_url: https://b13factory-garmentadv.netlify.app

# Identity settings
identity:
  roles: ['admin']
